@page
@model ProyectoBase.Pages.FicGestionEventos.CatProdServEspecifico.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.cat_prod_serv_especifico[0].IdProdServ)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.cat_prod_serv_especifico[0].ClaveProdServEsp)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.cat_prod_serv_especifico[0].DesProdServEsp)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.cat_prod_serv_especifico) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdProdServ)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ClaveProdServEsp)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DesProdServEsp)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
