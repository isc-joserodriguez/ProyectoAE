@page
@model ProyectoBase.Pages.FicGestionEventos.CatTiposEstatus.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.cat_tipos_estatus[0].DesTipoEstatus)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.cat_tipos_estatus[0].Activo)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.cat_tipos_estatus) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DesTipoEstatus)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Activo)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
