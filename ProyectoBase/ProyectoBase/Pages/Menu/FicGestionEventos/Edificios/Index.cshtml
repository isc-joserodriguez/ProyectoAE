@page
@model ProyectoBase.Pages.FicGestionEventos.Edificios.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.eva_cat_edificios[0].Clave)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.eva_cat_edificios[0].Alias)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.eva_cat_edificios[0].DesEdificio)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.eva_cat_edificios[0].Prioridad)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.eva_cat_edificios) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Clave)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Alias)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DesEdificio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Prioridad)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
